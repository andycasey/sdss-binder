sdss-binder @ git+https://github.com/andycasey/sdss-binder.git@f5841f388e9c5d35999a3ccfa79226d0bc74e534
numpy
pandas
scipy
h5py
corner
matplotlib
# Provides the `jupyter-matplotlib` frontend extension that `%matplotlib widget`
# needs. It must be installed when the image is built: installing it from a
# terminal in a running session does not register the extension.
ipympl>=0.9.4
marimo==0.24.0
# Adds the marimo entry to the JupyterLab launcher and proxies marimo through the
# Jupyter server. It ships a prebuilt labextension plus a jupyter-server-proxy
# config as package data, so like ipympl it only registers if it is installed
# when the image is built.
marimo-jupyter-extension>=0.4.0
# The extension ships a prebuilt labextension built against @jupyterlab/* ^4.5.6.
# The conda base env resolved jupyterlab 4.1.5, against which the federated
# extension will not load -- no marimo launcher icon and no *_mo.py file
# association. Verified: 4.6.3 reports "@marimo-team/jupyter-extension enabled
# OK", and notebook 6.5.6 / nbclassic still import alongside it.
jupyterlab>=4.5.6,<5
plotly
pip
lvm_tools>=0.1.0
matplotdrip
sdss-semaphore
pyarrow
tqdm
astropy
cmasher
spectracles>=0.6.4
fitsio
interpax
pyneb
altair
sdss-access
gdown
pydantic-ai-slim[anthropic,openai]
# anthropic/openai/mcp/pydantic-ai all pull anyio, unpinned, so a rebuild floats
# it forward. anyio >=4.12 made `anyio.abc` a lazy attribute, which httpcore
# <=1.0.5 dereferences at import time -- so `import httpcore` raises
# AttributeError, jupyterlab.labapp fails to import, /lab is never served, and
# the root URL silently falls back to the nbclassic /tree UI with no launcher.
# httpcore >=1.0.7 handles lazy anyio. Do not drop this pin.
httpcore>=1.0.7
anthropic
openai
mcp
pytest
websockets
python-lsp-server[all]
python-lsp-ruff
ruff
vegafusion[embed]>=1.5.0
vl-convert-python
